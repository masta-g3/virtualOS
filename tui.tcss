/* === Base - Warm dark theme === */
Screen {
    background: #0a0a0b;
}

/* === Header === */
#header {
    dock: top;
    height: 1;
    background: #141416;
    padding: 0 1;
}

#header-title {
    color: #e6a855;
    text-style: bold;
    width: auto;
}

#header-status {
    color: #e6a855;
    text-align: right;
    width: 1fr;
}

/* === Footer (keybindings) === */
#footer {
    dock: bottom;
    height: 1;
    background: #0a0a0b;
    color: #5c5a56;
    text-align: center;
    padding: 0 1;
}

/* === Messages Area === */
#messages {
    padding: 1;
    scrollbar-size: 1 1;
    scrollbar-background: #141416;
    scrollbar-color: #3a3a3d;
}

.user-message {
    height: auto;
    margin: 0 0 1 0;
}

.user-prefix {
    color: #e6a855;
    width: 2;
}

.user-text {
    color: #e8e6e3;
}

.agent-message {
    color: #9a9894;
    margin: 0 0 1 0;
    padding: 0;
}

.tool-call {
    color: #7aa2f7;
    margin: 0;
}

.tool-result {
    color: #6b7280;
    margin: 0;
}

.turn-separator {
    height: 1;
    margin: 0 0 1 0;
}

.error-message {
    color: #e85c5c;
}

.system-message {
    color: #5ce888;
    text-style: italic;
    margin: 0 0 1 0;
}

/* === Input === */
#prompt {
    dock: bottom;
    height: 3;
    background: #141416;
    border: tall #3a3a3d;
    padding: 0 1;
}

#prompt:focus {
    border: tall #e6a855;
}

/* === Selector Modal === */
SelectorScreen {
    align: center middle;
    background: rgba(10, 10, 11, 0.85);
}

#selector-title {
    color: #e6a855;
    text-style: bold;
    padding: 0 0 1 0;
    text-align: center;
}

#selector-list {
    width: auto;
    min-width: 40;
    max-height: 12;
    background: #141416;
    border: round #3a3a3d;
    padding: 0 1;
}

#selector-list:focus {
    border: round #e6a855;
}

#selector-list > .option-list--option-highlighted {
    background: #2a2a2d;
    color: #e6a855;
}
